# Environment Notes

The public validator uses only the Python standard library:

- `csv`
- `hashlib`
- `pathlib`
- `datetime`

The original private analysis environment used geospatial and scientific Python tooling to join official controls and create figures, but those raw-data processing steps are outside this public package because the raw datasets are not redistributed here.

